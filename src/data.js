  /*const filtrarDatos = (POTTER, condición) => {
  const filtrar = filtrar.filter(datap => datap.gender === condición);
  return filtrar; 
  }

  const filtrar = (POTTER, condición) => {
    const filtrar = POTTER.filter(element => element.rol === condición);
    return filtrar; 
    }
  

*/